pandas
nba_api
plotly
scikit-learn
flask
matplotlib
Pillow
requests

#if you want to install do pip install -r requirements.txt
#if you want to uninstall the packages for this requirements.txt file, do pip uninstall -r requirements.txt -y
